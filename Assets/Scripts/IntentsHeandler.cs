using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class IntentsHeandler : MonoBehaviour
{

    //public TMP_Text dddddd;

    //private void Start()
    //{
    //    // Получение данных из Intent
    //    AndroidJavaClass unityPlayerClass = new AndroidJavaClass("com.unity3d.player.UnityPlayer");
    //    AndroidJavaObject currentActivity = unityPlayerClass.GetStatic<AndroidJavaObject>("currentActivity");
    //    AndroidJavaObject intent = currentActivity.Call<AndroidJavaObject>("getIntent");
    //    string action = intent.Call<string>("getAction");
    //    string type = intent.Call<string>("getType");
    //    string dataString = intent.Call<string>("getDataString");

    //    // Обработка полученных данных
    //    if (action == "android.intent.action.VIEW" && type != null && dataString != null)
    //    {
    //        Debug.Log("Открыт файл с типом: " + type);
    //        Debug.Log("Путь к файлу: " + dataString);

    //        dddddd.text = type;
    //        dddddd.text += " \n" + dataString;

    //    }
    //}
}
